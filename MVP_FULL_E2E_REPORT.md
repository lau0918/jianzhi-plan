# MVP 完整性测试报告（人工路径）

测试时间：2026-04-10 08:43 CST  
测试环境：`/tmp/mvp_full_e2e_1775781672`（隔离，不污染正式数据）  
测试方式：按终端用户真实操作路径逐步调用网页与 API

## 结论

MVP 核心闭环通过，可用于小范围真实用户验证。

## 测试步骤与结果

1. 打开首页：通过（HTTP 200）
2. 初始状态读取：通过（目标未设置、今日未记录）
3. 设置进食窗口：通过（`07:00 + 8小时`）
4. 设置周期目标：通过（`2026-04-01 ~ 2026-06-01`, `100 -> 80`）
5. 记录进食（窗口内 + 窗口外）：通过（`in_window` 判定准确）
6. 记录体重：通过（`89.0kg`）
7. 读取最终状态：通过
   - 目标差值：`left_weight = 9.0`
   - 周期状态：`in_progress`
   - 周期天数：`10/62`，剩余 `52`
   - 今日状态：`未达标`，窗口外 `1` 次
   - 最近进食：`2026-04-10 21:11 炸鸡`
8. Excel 导出文件：通过（`fasting_report.xlsx` 生成）
9. Excel 工作表结构：通过（`Summary/FastingRecords/MealRecords/WeightRecords/DailySummary`）

## 发现项

1. `goal.progress_percent` 返回 `null`（非阻塞）
   - 当前前端目标进度由前端按 `start/current/target` 计算，不依赖该字段。
   - 建议后续统一：要么后端补齐该字段，要么删掉该返回字段，避免歧义。

2. `week_stats.bad_days` 返回 `null`（非阻塞）
   - 当前页面已不依赖该字段。
   - 建议后续统一周统计字段命名与完整性。

## 证据日志

- 完整日志：`/tmp/mvp_full_e2e_1775781672/E2E_RUN_LOG.txt`
- 首页抓取样本：`/tmp/mvp_full_e2e_1775781672/home.html`
